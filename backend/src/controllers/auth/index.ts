/**
 * @fileoverview Auth sub-controllers barrel export
 * @description Barrel export for auth sub-controllers
 * @module controllers/auth
 *
 * @example
 * // English: Import from sub-controllers
 * import { me } from '../controllers/auth';
 *
 * // Español: Importar desde sub-controladores
 * import { me } from '../controllers/auth';
 */

// Profile controller
export { me } from './ProfileController';
